<form method="post" action="{{ path('app_entree_depot_delete', {'id': entree_depot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entree_depot.id) }}">
    <button class="glow-on-hover">Supprimer</button>
</form>
