<form method="post" action="{{ path('app_sortie_depot_delete', {'id': sortie_depot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sortie_depot.id) }}">
    <button class="glow-on-hover">Supprimer</button>
</form>
